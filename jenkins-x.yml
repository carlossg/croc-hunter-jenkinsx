buildPack: go
pipelineConfig:
  env:
  - name: ORG
    value: go-exchange-staging
  pipelines:
    pullRequest:
      build:
        replace: true
        steps:
        - env:
          - name: ORG
            value: go-exchange-staging
          # command: make linux && VERSION=$PREVIEW_VERSION skaffold build -f skaffold.yaml
      postBuild:
        replace: true
        steps:
        - env:
          - name: ORG
            value: go-exchange-staging
          command: jx step post build --image $DOCKER_REGISTRY/$ORG/$APP_NAME:$PREVIEW_VERSION