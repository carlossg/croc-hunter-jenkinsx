buildPack: go
pipelineConfig:
  env:
  - name: ORG
    value: go-exchange-staging
  pipelines:
    pullRequest:
      build:
        # replace: true
        steps:
        - env:
          - name: ORG
            value: go-exchange-staging
          # command: export VERSION=$PREVIEW_VERSION && skaffold build -f skaffold.yaml
      postBuild:
        # replace: true
        steps:
        - env:
          - name: ORG
            value: go-exchange-staging
          # command: export VERSION=$PREVIEW_VERSION && skaffold build -f skaffold.yaml