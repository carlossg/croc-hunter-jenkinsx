buildPack: go
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        preSteps:
        - command: cat skaffold.yaml
        # replace: true
        steps:
        - command: export VERSION=$PREVIEW_VERSION && skaffold build -f skaffold.yaml
          env:
          - name: ORG
            value: go-exchange-staging
