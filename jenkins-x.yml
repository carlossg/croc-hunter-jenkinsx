buildPack: go
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        replace: true
        steps:
        - command: export VERSION=$PREVIEW_VERSION && skaffold build -f skaffold-temp.yaml
