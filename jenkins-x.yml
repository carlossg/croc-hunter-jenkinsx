buildPack: go
pipelineConfig:
  env:
  - name: ORG
    value: go-exchange-staging
  - name: DOCKER_REGISTRY
    value: asia.gcr.io
  pipelines:
    pullRequest:
      build:
        replace: true
        steps:
        - command: make linux
        - command: VERSION=$PREVIEW_VERSION skaffold build -f skaffold.yaml
      postBuild:
        replace: true
        steps:
        - command: jx step post build --image $DOCKER_REGISTRY/$ORG/$APP_NAME:$PREVIEW_VERSION