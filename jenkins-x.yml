buildPack: go
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        replace: true
        steps:
        - command: export VERSION=$PREVIEW_VERSION && export ORG=go-exchange-staging
            && skaffold build -f skaffold.yaml
